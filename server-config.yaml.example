# Rate limiting
throttle:
  # Time-to-live (TTL) in seconds
  ttl:
  # Amount of requests client can make in given TTL
  limit:

# Default operator PDF language - options: 'cs', 'en'
pdfOperatorLanguageCode:

# Enable CORS for our web app URL
webUrl:

# OIDC authentication
oidc:
  # JPM (Jednotné přihlášení MUNI) - find app here: https://spreg.aai.muni.cz/auth/facilities/myServices
  jpm:
    clientId:
    clientSecret:
    introspectionEndpoint:
