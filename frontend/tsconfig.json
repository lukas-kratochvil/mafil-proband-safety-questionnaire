{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    /* Base options */
    "target": "ES2022",
    /* If transpiling with TypeScript (tsc) */
    "module": "ESNext",
    "moduleResolution": "Node", // don't set 'moduleResolution' to 'Bundler' as it is intended for compilation with bundlers
    "outDir": "./dist",
    "noEmit": true,
    /* Type checking */
    // TODO: 'verbatimModuleSyntax' set to 'true' does collide with ESLint - ESLint will crash
    // "verbatimModuleSyntax": true, // ensures the right usage of ES module syntax (related to the 'module' setting)
    "allowSyntheticDefaultImports": true, // enables imports like this 'import X from Y' instead of 'import * as X from Y' when module doesn't have a default import
    /* If code runs in the DOM */
    "lib": [
      "DOM",
      "DOM.Iterable",
      "ESNext",
    ],
    /* For React app */
    "jsx": "react-jsx",
    /* For Vitest */
    "types": [
      "vitest/globals", // enable using Vitest globals without importing them (https://vitest.dev/config/#globals)
    ],
    /* Custom imports mapping */
    "baseUrl": "./",
    "paths": {
      "@test/*": [
        "src/test/*",
      ],
      "@app/*": [
        "src/*",
      ],
    },
  },
  "include": [
    "src",
  ],
  "exclude": [
    "src/**/*.dev.[ts, tsx]",
    "src/**/__tests__/**/*.[ts, tsx]",
    "src/**/__mocks__/**/*.[ts, tsx]",
  ],
  "references": [
    {
      "path": "./tsconfig.node.json",
    },
  ],
}
